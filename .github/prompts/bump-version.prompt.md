---
mode: agent
description: Bump the OmniTerm version — sync all version files, generate CHANGELOG.md, commit, and tag the release.
---

# Bump Version

Target version: `${input:version:e.g. v0.1.2}`

The canonical instructions for this task live at
[`.agents/skills/bump-version/SKILL.md`](../../.agents/skills/bump-version/SKILL.md).

Read that file and follow it exactly. This file is a tool-specific pointer only —
do not duplicate the procedure here, so Claude Code, Copilot, and opencode stay
in sync with one source of truth.
