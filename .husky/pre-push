# Verify the locked GitHub identity and local-only commits before spending time on the Test Gate.
node scripts/identity-guard.mjs pre-push "$1" "$2"

# Refuse a push that would fail the Test Gate. See scripts/pre-push-check.mjs.
node scripts/pre-push-check.mjs
