<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256" width="256" height="256" role="img" aria-label="OmniTerm logo (placeholder)">
  <!-- ============================================================== -->
  <!-- PLACEHOLDER LOGO — replace with the real OmniTerm artwork.      -->
  <!-- Also replace: src/assets/logo.svg (in-app logo),                -->
  <!-- build/icon.ico and build/icon.png (installer & taskbar icon).   -->
  <!-- ============================================================== -->
  <rect x="8" y="8" width="240" height="240" rx="52" fill="#1a1b26"/>
  <rect x="8" y="8" width="240" height="240" rx="52" fill="#7aa2f7" fill-opacity="0.06"
        stroke="#7aa2f7" stroke-opacity="0.5" stroke-width="3"/>
  <!-- "N" monogram, drawn as a connection path between two nodes -->
  <path d="M86 178 V94 L170 178 V94" fill="none" stroke="#7aa2f7"
        stroke-width="16" stroke-linecap="round" stroke-linejoin="round"/>
  <!-- connection nodes (start / end) -->
  <circle cx="86" cy="94" r="13" fill="#89ddff"/>
  <circle cx="170" cy="178" r="13" fill="#9ece6a"/>
</svg>
